<?php

    /**Com a função array_reverse podemos obter o array inverso;
     * Passamos apenas o array como argumento;
     * O retorno será o array invertido;
    */

    $arr = [1,2,3,4,5,6,7,8,9];

    $inverso = array_reverse($arr);

    print_r($inverso);