<?php

    /**O HTTP é um protocolo que não mantém o estado (stateless);
     * Ou seja, após o fim da requisição a conexão entre usuário e servidor é FINALIZADA,
     * a próxima conexão não possui mais relação entre ambos;
     * Para conseguir manter esses dados podemos utilizar os cookies;
     * O problema dessa abordagem é que alguns navegadores não permitem o uso de cookies
     * ou os bloqueiam;
    */

    

?>