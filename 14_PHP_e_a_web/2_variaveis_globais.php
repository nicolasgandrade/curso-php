<?php

    /**
     * $_ENV => variáveis de ambiente;
     * $_GET => Parâmetros enviados por request GET;
     * $_POST => Parâmetros enviado por POST;
     * $_COOKIE = Valores de cookies;
     * $_SERVER => informações sobre o servidor;
     * $_FILES => Informações sobre arquivos que vieram de uploads;
    */